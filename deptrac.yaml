deptrac:
  paths:
    - src/
  exclude_files: []
  layers:
    - name: Src
      collectors:
        - type: directory
          value: src/.*
    - name: Vendor
      collectors:
        - type: directory
          value: vendor/.*
  ruleset:
    Src:
      - Src
