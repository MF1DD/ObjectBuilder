{
    "name": "mf1dd/object-builder",
    "description": "Automatically creates fully populated PHP object instances with random, type-safe values. Designed for test fixtures and runtime object generation. Supports all PHP class types — regular classes, readonly classes, abstract classes, enums, traits, interfaces, and built-in stock classes like DateInterval or ArrayObject. Recursively resolves nested object graphs, detects property names for semantically meaningful values (timezone, email, firstname …), and offers constraint-based value ranges via a fluent API. No framework required; zero runtime dependencies beyond nikic/php-parser. PHP 8.2+.",
    "type": "library",
    "license": "MIT",
    "keywords": ["object", "builder", "fixture", "test", "random", "data", "generator", "mock", "reflection", "factory"],
    "support": {
        "issues": "https://github.com/MF1DD/ObjectBuilder/issues",
        "source": "https://github.com/MF1DD/ObjectBuilder"
    },
    "require": {
        "php": "^8.2",
        "nikic/php-parser": "^5.0"
    },
    "autoload": {
        "psr-4": {
            "MF1DD\\Domain\\": "src/Domain/",
            "MF1DD\\Application\\": "src/Application/",
            "MF1DD\\Infrastructure\\": "src/Infrastructure/",
            "MF1DD\\UserInterface\\": "src/UserInterface/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "MF1DD\\Tests\\": "tests/"
        }
    },
    "require-dev": {
        "phpunit/phpunit": "^10.4.2",
        "phpstan/phpstan": "^2.1",
        "symplify/easy-coding-standard": "^12.1",
        "infection/infection": "^0.30",
        "roave/security-advisories": "dev-latest",
        "rector/rector": "^2.1",
        "vimeo/psalm": "^6.13",
        "deptrac/deptrac": "^3.0"
    },
    "authors": [
        {
            "name": "MF1DD",
            "email": "packagist@mf1dd.com"
        }
    ],
    "config": {
        "allow-plugins": {
            "infection/extension-installer": true
        }
    }
}
